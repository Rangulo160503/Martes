@model CEGA.Models.Egreso
@{
    ViewData["Title"] = "Editar egreso";
}

<div class="mb-3">
    <h4 class="mb-1">Editar egreso</h4>
    <p class="text-muted mb-0">Actualiza el monto, la fecha y el concepto.</p>
</div>

<div class="card">
    <div class="card-body">
        <form asp-action="EditarEgreso" asp-controller="Contabilidad" method="post" class="row g-3" novalidate>
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />

            <div class="col-md-4">
                <label asp-for="Monto" class="form-label">Monto</label>
                <div class="input-group">
                    <span class="input-group-text">₡</span>
                    <input asp-for="Monto" class="form-control" type="number" step="0.01" min="0" placeholder="0.00" />
                </div>
                <span asp-validation-for="Monto" class="text-danger"></span>
            </div>

            <div class="col-md-4">
                <label asp-for="Fecha" class="form-label">Fecha</label>
                <input asp-for="Fecha" class="form-control" type="datetime-local" asp-format="{0:yyyy-MM-ddTHH:mm}" />
                <span asp-validation-for="Fecha" class="text-danger"></span>
            </div>

            <div class="col-12">
                <label asp-for="Concepto" class="form-label">Concepto</label>
                <textarea asp-for="Concepto" class="form-control" rows="3" placeholder="Descripción del egreso"></textarea>
                <span asp-validation-for="Concepto" class="text-danger"></span>
            </div>

            <div class="col-12 d-flex gap-2">
                <button type="submit" class="btn btn-accent">Guardar</button>

                <!-- Cancelar: vuelve SIEMPRE al listado de Egresos -->
                <a asp-controller="Contabilidad"
                   asp-action="Egresos"
                   class="btn btn-outline-secondary">
                    Cancelar
                </a>

                @* Alternativa (si prefieres botón que navega sin depender de rutas por defecto):
                <button type="button"
                        class="btn btn-outline-secondary"
                        onclick="window.location.href='@Url.Action("Egresos","Contabilidad")'">
                    Cancelar
                </button>
                *@
            </div>

            <div class="col-12">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
