@{
    ViewData["Title"] = "Buscar Comentario";
}

<h2>Buscar Comentario en Proyecto</h2>

<form asp-action="Buscar" method="post">
    <input type="hidden" name="proyectoId" value="@ViewBag.ProyectoId" />
    <div>
        <label>Nombre del Comentario:</label>
        <input type="text" name="nombreComentario" />
        <button type="submit">Buscar</button>
    </div>
</form>

@if (ViewBag.Mensaje != null)
{
    <div class="text-danger">@ViewBag.Mensaje</div>
}
